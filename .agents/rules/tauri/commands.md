# Tauriコマンド

- 権限を伴うOS・ファイルシステム操作はフロントエンドへ移さず、
  Tauriコマンド内に置くこと。
- コマンドは一つの明確な操作を担当し、フロントエンド固有の表示状態を
  持たせないこと。
- コマンド名やpayloadを変更する場合は、対応するフロントエンドの `invoke` を
  同じ変更内ですべて更新すること。
- Rust側の引数名とfrontendから渡すpayloadのキーが一致していることを
  確認すること。
- コマンドで返すエラーは、frontendで安全に文字列化して利用できる形に
  保つこと。
